// lib/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  // 🎨 SAO-Themed App Color Palette

  // Primary Colors (Main UI)
  static const Color deepBlue = Color(0xFF003A8F);
  static const Color royalBlue = Color(0xFF1E5BB8);

  // Accent Colors (Important actions)
  static const Color gold = Color(0xFFFFC72C);
  static const Color lightGold = Color(0xFFFFE08A);

  // Neutral Colors (Clean look)
  static const Color white = Color(0xFFFFFFFF);
  static const Color lightGray = Color(0xFFF5F7FA);
  static const Color darkGray = Color(0xFF333333);
}